import { FormEvent, useState } from 'react';
import { generateText } from './ai.api';
export function AiPanel(){const[prompt,setPrompt]=useState('Give me three practical DDD aggregate design checks.');const[output,setOutput]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{setOutput(await generateText(prompt));}catch(v){setError(v instanceof Error?v.message:'AI request failed.');}finally{setLoading(false);}}return <section className="card"><div className="section-heading"><div><p className="eyebrow">Assistant</p><h2>AI playground</h2></div><span className="pill">Live API</span></div><form className="stack" onSubmit={submit}><label>Prompt<textarea rows={5} value={prompt} onChange={e=>setPrompt(e.target.value)}/></label><button className="primary" disabled={loading}>{loading?'Generating…':'Generate'}</button></form>{error&&<p className="error surface-error">{error}</p>}{output&&<pre className="ai-output">{output}</pre>}</section>}
