import { Component,inject } from '@angular/core';import { FormsModule } from '@angular/forms';import { AiStore } from './ai.store';
@Component({selector:'app-ai-page',standalone:true,imports:[FormsModule],template:`<section class="card"><div class="section-heading"><div><p class="eyebrow">Assistant</p><h2>AI playground</h2></div><span class="pill">Live API</span></div><form class="stack" (ngSubmit)="store.generate(prompt)"><label>Prompt<textarea name="prompt" rows="5" [(ngModel)]="prompt"></textarea></label><button class="primary" [disabled]="store.loading()">{{store.loading()?'Generating…':'Generate'}}</button></form>@if(store.output()){<pre class="ai-output">{{store.output()}}</pre>}</section>`})export class AiPage{readonly store=inject(AiStore);prompt='Give me three practical DDD aggregate design checks.';}
