import { Component, computed, input, output } from '@angular/core';
import { AuthUser } from '../../core/auth/auth-session.service';
@Component({selector:'app-shell',standalone:true,template:`<main class="shell"><header class="topbar"><div class="brand"><div class="brand-mark small">JV</div><div><strong>TemplateApp</strong><span>Mini Store Admin</span></div></div><div class="account"><div class="avatar-fallback">{{initials()}}</div><div class="account-copy"><strong>{{user().displayName}}</strong><span>{{user().email}} · {{user().role}}</span></div><button class="ghost" (click)="logout.emit()">Sign out</button></div></header><ng-content /></main>`})
export class AppShellComponent { readonly user=input.required<AuthUser>();readonly logout=output<void>();readonly initials=computed(()=>this.user().displayName.split(/\s+/).filter(Boolean).slice(0,2).map(p=>p[0]?.toUpperCase()).join('')||'U'); }
