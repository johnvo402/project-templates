import { ChangeDetectionStrategy, Component, input } from '@angular/core';
import { MatCardModule } from '@angular/material/card';
import { formatCurrency } from '../../../utils/formatters';
import type { DashboardTopProduct } from '../dashboard.models';

@Component({ selector:'app-top-products-panel', standalone:true, imports:[MatCardModule], changeDetection:ChangeDetectionStrategy.OnPush, styles:[`mat-card{height:100%;border-radius:16px}mat-card-content{padding:20px}h2,p{margin:0}p{margin-top:4px;color:var(--mat-sys-on-surface-variant)}.rows{margin-top:16px}.row{display:grid;grid-template-columns:34px minmax(0,1fr) auto;gap:12px;align-items:center;padding:12px 0;border-top:1px solid var(--mat-sys-outline-variant)}.row:first-child{border-top:0}.name{min-width:0}.rank{color:var(--mat-sys-on-surface-variant);font-size:12px}.empty{padding:32px 0;text-align:center;color:var(--mat-sys-on-surface-variant)}`], template:`<mat-card appearance="outlined"><mat-card-content><h2>Top products</h2><p>Products ranked by revenue.</p><div class="rows">@for(item of products().slice(0,6);track item.productName;let i=$index){<div class="row"><span class="rank">#{{i+1}}</span><strong class="name">{{item.productName}}</strong><strong>{{money(item.revenue)}}</strong></div>}@empty{<div class="empty">No sales data yet.</div>}</div></mat-card-content></mat-card>` })
export class TopProductsPanelComponent { readonly products=input.required<DashboardTopProduct[]>(); readonly money=formatCurrency; }
