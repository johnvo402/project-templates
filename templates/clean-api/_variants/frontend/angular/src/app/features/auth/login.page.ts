import { Component, inject, output } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { AuthSessionService, AuthUser } from '../../core/auth/auth-session.service';
@Component({selector:'app-login-page',standalone:true,imports:[FormsModule],template:`
<main class="auth-layout"><section class="auth-hero"><div class="brand-mark">JV</div><p class="eyebrow">JohnVo starter</p><h1>Build the product, not the boilerplate.</h1><p>DDD backend, secure auth, feature-based frontend, paging and validation are already wired.</p><div class="hero-points"><span>Clean Architecture</span><span>Vertical Slice</span><span>Permission policies</span></div></section>
<section class="auth-card"><div class="segmented"><button type="button" [class.active]="mode==='login'" (click)="mode='login'">Sign in</button><button type="button" [class.active]="mode==='register'" (click)="mode='register'">Create account</button></div><p class="eyebrow">Welcome</p><h2>{{mode==='login'?'Sign in to your workspace':'Create the first account'}}</h2><form class="stack" (ngSubmit)="submit()">@if(mode==='register'){<label>Display name<input name="displayName" [(ngModel)]="displayName" /></label>}<label>Email<input name="email" type="email" [(ngModel)]="email" /></label><label>Password<input name="password" type="password" [(ngModel)]="password" /></label>@if(mode==='register'){<p class="hint">The first account becomes Admin.</p>}@if(error){<p class="error surface-error">{{error}}</p>}<button class="primary large" [disabled]="loading">{{loading?'Working…':mode==='login'?'Sign in':'Create account'}}</button></form></section></main>`})
export class LoginPage { private readonly auth=inject(AuthSessionService); readonly authenticated=output<AuthUser>(); mode:'login'|'register'='login';displayName='Admin';email='admin@example.com';password='ChangeMe123!';error='';loading=false;async submit(){this.loading=true;this.error='';try{const u=this.mode==='login'?await this.auth.login(this.email,this.password):await this.auth.register(this.email,this.password,this.displayName);this.authenticated.emit(u);}catch(e){this.error=e instanceof Error?e.message:'Authentication failed.';}finally{this.loading=false;}}}
