import { Component,inject,OnInit } from '@angular/core';import { FormsModule } from '@angular/forms';import { firstValueFrom } from 'rxjs';import { AuthSessionService } from '../../core/auth/auth-session.service';import { ProfileApiService } from './profile-api.service';
@Component({selector:'app-profile-page',standalone:true,imports:[FormsModule],template:`<section class="card profile-card"><div class="section-heading"><div><p class="eyebrow">Account</p><h2>Profile</h2></div><span class="status-dot">Active</span></div>@if(loading){<p class="muted">Loading profile…</p>}@else{<form class="stack" (ngSubmit)="save()"><label>Display name<input name="displayName" maxlength="120" [(ngModel)]="displayName" /></label><label>Email<input [value]="email" disabled /></label><label>Bio<textarea name="bio" rows="4" maxlength="500" [(ngModel)]="bio"></textarea></label><div class="form-footer"><span class="hint">{{bio.length}}/500</span><button class="primary" [disabled]="saving">{{saving?'Saving…':'Save profile'}}</button></div>@if(notice){<p class="notice">{{notice}}</p>}</form>}</section>`})
export class ProfilePage implements OnInit{private readonly api=inject(ProfileApiService);private readonly auth=inject(AuthSessionService);displayName='';email='';bio='';loading=true;saving=false;notice='';async ngOnInit(){try{const p=await firstValueFrom(this.api.get());this.displayName=p.displayName;this.email=p.email;this.bio=p.bio??'';}finally{this.loading=false;}}async save(){this.saving=true;this.notice='';try{await firstValueFrom(this.api.update(this.displayName,this.bio));this.auth.updateDisplayName(this.displayName.trim());this.notice='Profile updated.';}catch{this.notice='Could not update profile.';}finally{this.saving=false;}}}
